.env
.env.local
.venv/
__pycache__/
*.pyc
*.pyo
logs/*.log
.uv/
dist/
*.egg-info/

# Firebase secrets — never commit
secrets/
serviceAccountKey.json
firebase-service-account.json

# MT5 startup config — generated at runtime, contains plaintext password
config/mt5_autologin.ini
*.service-account.json

# Journal runtime files
journal_retry_queue.jsonl
journal_pending_deals.jsonl
generated_screenshots/

# macOS
.DS_Store

# Claude Code runtime files
.agents/
.claude/
skills-lock.json

# Per-broker symbol resolution cache (runtime, machine-specific)
config/symbol_cache_*.json

# Per-account trading-fee cycle anchor (runtime, machine-specific)
config/fee_anchor_*.json
